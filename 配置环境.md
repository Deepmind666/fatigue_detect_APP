# Android 项目环境配置记录

本文档记录了将 Android 项目迁移到新机器后，配置开发环境的完整步骤。 

## 1. 检查 Java 开发工具包 (JDK)

根据项目配置文件 `app/build.gradle.kts`，本项目需要 **Java 8 (JDK 1.8)**。

请打开 PowerShell 终端并执行以下命令来检查您的 Java 版本：

```powershell
java -version
```

如果输出的版本信息不是 1.8.x，或者命令未找到，则需要安装或切换到正确的 JDK 版本。

接下来，检查 `JAVA_HOME` 环境变量是否已设置。此变量应指向您的 JDK 8 安装目录。

```powershell
echo $env:JAVA_HOME
```

如果 `JAVA_HOME` 未设置或指向了错误的 JDK 版本，请进行相应的设置。例如，如果您的 JDK 8 安装在 `C:\Program Files\Java\jdk1.8.0_xxx`，可以这样设置：

```powershell
# 临时设置环境变量（只在当前终端窗口有效）
$env:JAVA_HOME = "C:\Program Files\Java\jdk1.8.0_xxx"
$env:Path = "$env:JAVA_HOME\bin;" + $env:Path

# 如需永久设置，请通过系统的"编辑系统环境变量"界面操作。
```

---

## 2. 配置 Android SDK

Android 项目需要指定 Android SDK 的路径。此路径通常保存在项目根目录下的 `local.properties` 文件中，并且因机器而异。

首先，我将检查项目中是否存在 `local.properties` 文件以及其内容。

`local.properties` 文件已存在，但内容有重复，并且 SDK 路径 `D:\\Android\\android_sdk` 很可能不是您当前机器上的有效路径。

您需要找到您机器上 Android SDK 的实际安装路径。通常可以在 Android Studio 的设置中找到：
1. 打开 Android Studio。
2. 进入 `File > Settings > Appearance & Behavior > System Settings > Android SDK` (在较新版本中可能是 `File > Settings > Languages & Frameworks > Android SDK`)。
3. 在顶部您会看到 `Android SDK Location`，这就是您需要的路径。

找到路径后，需要更新 `local.properties` 文件。

我将为您清理并更新 `local.properties` 文件。请将下面的占位符路径替换为您的实际 SDK 路径。

---

## 3. 验证配置并构建项目

在您将 `local.properties` 文件中的 `sdk.dir` 替换为正确的 Android SDK 路径后，我们就可以尝试构建项目了。

这将是检验 JDK 和 Android SDK 配置是否成功的关键一步。Gradle 会开始下载所有必需的依赖项，这在第一次构建时可能会花费一些时间。

请在项目的根目录打开 PowerShell 终端，然后执行以下命令：

```powershell
./gradlew.bat clean
```

如果一切顺利，您应该会看到 `BUILD SUCCESSFUL` 的消息。如果出现错误，请检查之前的步骤，特别是 `JAVA_HOME` 和 `sdk.dir` 的路径是否正确无误。
