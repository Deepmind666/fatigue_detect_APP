# 前端和后端通信知识讲解和通信操作

## 第一部分：通信基础知识与协议解析

对于初学者来说，App (前端) 和硬件 (后端) 通信可能听起来很复杂，但我们可以把它想象成两个人在用对讲机说话。为了让对话顺利进行，他们需要遵守一套规则，这就是"通信协议"。

### 1. 什么是串口通信？

想象一下，数据就像一队士兵，需要从 App（A 点）走到果汁机硬件（B 点）。

*   **并行通信**：像一个八车道的高速公路，8 个士兵可以并排同时出发，速度快，但需要建 8 条路，成本高（需要很多根线）。
*   **串口通信**：像一条单行道，所有士兵必须排成一队，一个接一个地走。速度相对慢一些，但只需要一根"路"就行了，简单、成本低、可靠。

我们的果汁机用的就是**串口通信**，这在物联网设备中非常普遍。安卓手机通过 USB 口连接一个"USB 转串口"模块，就可以和果汁机的主控板进行串口通信了。

### 2. 什么是数据包？

我们在说话时，会把一句话说完，对方才知道我们表达了一个完整的意思。计算机通信也是一样，它会把要发送的数据打包成一个"数据包"（Data Packet）。对方只有收到了一个完整的数据包，才会进行处理。

根据我们的文档，一个标准的数据包是 7 个字节（可以想象成 7 个字）：

`FF 00 00 00 00 00 FE`

*   **包头 (Header) - FF**：这就像说话前的"喂，请注意！"。它告诉接收方："注意，我要开始发数据了！"。`FF` 是一个固定的信号，硬件一旦检测到它，就知道一个新命令要来了。
*   **包尾 (Trailer) - FE**：这就像一句话末尾的句号。它告诉接收方："我说完了。"。当硬件收到 `FE`，它就知道一个数据包接收完毕，可以开始处理这条指令了。
*   **中间的 5 个字节**：这就是我们具体要传达的"指令内容"。

### 3. 什么是十六进制？

我们日常用的是十进制（0-9）。但计算机底层打交道的是二进制（0和1）。为了更方便地表示二进制，程序员发明了十六进制（0-9 和 A-F）。

*   **关系**：1 个十六进制位，正好可以表示 4 个二进制位。2 个十六进制位（例如 `FF`）就代表 8 个二进制位，也就是 1 个**字节 (Byte)**。
*   **范围**：1 个字节能表示的数字范围是 0 到 255（十进制）。
*   **文档中的例子**：
    *   `C8` (十六进制) = 200 (十进制)
    *   `B4` (十六进制) = 180 (十进制)
    *   `96` (十六进制) = 150 (十进制)
    *   `64` (十六进制) = 100 (十进制)

在我们的协议里，所有数据都用十六进制表示，因为这和计算机底层的字节存储方式完全对应，处理起来非常高效。

### 4. 协议深度解析

现在我们来详细分析两条指令。

#### A. 用户指令 (制作果汁)

*   **格式**: `FF 00 <数据1> <数据2> <数据3> <数据4> FE`
*   **第二位 `00`**: 代表这是给普通用户用的指令。
*   **核心数据**: 中间的四个字节代表了具体的配料用量。根据我们 App 的设计，制作一杯果汁需要三个核心数据：**冰 (ice)**、**果汁 (juice)** 和 **水 (water)**。
*   **解析与适配**: 我们的协议有 4 个数据位，但我们只需要 3 个。这很常见，通常我们会将第 4 个数据位保留，填 `00` 即可。所以，制作一杯"中杯"果汁（假设配比是 冰130g, 果汁80g, 水120g），我们发送的数据包应该是：
    *   `FF`: 包头
    *   `00`: 用户指令
    *   `82`: 130 的十六进制
    *   `50`: 80 的十六进制
    *   `78`: 120 的十六进制
    *   `00`: 保留位
    *   `FE`: 包尾
    *   **完整指令**: `FF 00 82 50 78 00 FE`
*   **安全限制**: 文档提到"超 254 报警"，这意味着我们传的每个配料值都不能超过 254。我们 App 在发送前需要做一个检查。

#### B. 管理员指令 (机器自清洗)

*   **格式**: `FF 01 00 00 00 00 FE`
*   **第二位 `01`**: 代表这是管理员指令。
*   **核心数据**: `00 00 00 00`。这里，这四个字节没有具体数值含义，它们整体代表一个固定的动作——"自清洗"。当硬件收到包头、确认是管理员指令后，看到这四个零，就会执行清洗程序。

### 5. 我们 App 要怎么做？

我们的 App 需要一个"翻译官"+"通信员"的角色。这个角色就是 `HardwareManager.kt`。它的职责是：
1.  **连接硬件**：通过 USB 口找到果汁机，并建立稳定的串口通信连接。
2.  **翻译指令**：把 App 的操作（例如"用户点了中杯"）翻译成硬件能懂的字节码（例如 `FF 00 82 50 78 00 FE`）。
3.  **发送指令**：把这一串字节码通过串口发给硬件。

---

## 第二部分：详细操作计划

为了实现上述功能，我将按以下步骤，逐步为我们的 App 添加通信能力。

1.  **引入"串口通信"库**
    *   **目标**: 我们需要一个现成的工具库来帮我们处理复杂的 USB 和串口通信细节。`usb-serial-for-android` 是一个非常流行和可靠的选择。
    *   **操作**: 我会在项目的 `build.gradle.kts` 和 `libs.versions.toml` 文件中添加这个库的依赖。

2.  **声明"USB 使用"权限**
    *   **目标**: App 需要向安卓系统明确表示，它需要使用 USB 功能来连接外部设备。
    *   **操作**:
        *   在 `AndroidManifest.xml` 文件中，添加 `<uses-feature android:name="android.hardware.usb.host" />` 来声明 App 需要 USB 主机功能。
        *   同样在此文件中，为 `MainActivity` 添加一个意图过滤器（intent-filter），这样当果汁机插入手机时，我们的 App 可以被系统自动识别甚至启动。

3.  **创建"设备过滤器"**
    *   **目标**: 告诉安卓系统，我们的 App 只关心特定的 USB 设备（也就是我们的果汁机），而不是任何随便插入的 U 盘或鼠标。每个 USB 设备都有一个**供应商 ID (VID)** 和**产品 ID (PID)**，就像人的身份证号。
    *   **操作**: 我会创建一个 `device_filter.xml` 文件。**注意：我会先用通用的占位符 ID，之后您需要从硬件工程师那里获取真实的 VID 和 PID 并替换它。**

4.  **"武装" `HardwareManager.kt`**
    *   **目标**: 这是最核心的一步。我要把目前还是空壳的 `HardwareManager.kt` 变成一个功能强大的硬件通信中心。
    *   **操作**:
        *   添加连接和断开串口的逻辑。
        *   创建 `sendMakeJuiceCommand(ice: Int, juice: Int, water: Int)` 函数。它会接收冰、果汁、水的克数，转换成字节，打包成 `FF 00 ... FE` 的格式，然后发送出去。
        *   创建 `sendCleanCommand()` 函数。它会构建并发送 `FF 01 00 00 00 00 FE` 指令。

5.  **联动 ViewModel**
    *   **目标**: 当用户在界面上点击按钮时，`ViewModel` 需要调用 `HardwareManager` 去发送真正的指令。
    *   **操作**:
        *   修改 `DrinkMenuViewModel.kt` 中的 `confirmCustomization` 函数。在用户确认杯型后，它不再只是打印日志，而是会调用 `HardwareManager.sendMakeJuiceCommand()`。
        *   在 `DrinkMenuViewModel.kt` 中新增一个 `cleanMachine()` 函数，专门用于调用 `HardwareManager.sendCleanCommand()`。

6.  **连接界面按钮**
    *   **目标**: 将界面上的按钮和 `ViewModel` 中的新功能连接起来。
    *   **操作**:
        *   **管理员界面**: 根据协议，清洗是管理员功能。我会在 `AdminScreen.kt` 上增加一个"一键清洗"按钮。
        *   **主活动 (`MainActivity.kt`)**: 将 `AdminScreen` 上新按钮的点击事件，绑定到 `ViewModel` 的 `cleanMachine()` 函数上。
        *   **主界面**: 原来主界面上的"一键清洗"按钮，我会暂时保留它的 `TODO` 状态，因为协议指明这是管理员功能。这为您后期与后端联调留下了调整空间。

在完成所有这些编码步骤后，我会把具体的代码改动记录在本文档的第三部分，作为您学习和参考的实例。

现在，我将开始执行这个计划。

---

## 第三部分：代码实现参考

以下是为实现通信功能而修改的核心代码片段。您可以对照这些代码，理解每个文件是如何协同工作的。

### 1. Gradle 配置 (`app/build.gradle.kts`)

我们添加了 `usb-serial-for-android` 库，并指定了 `jitpack.io` 仓库地址。

```kotlin
// ... in app/build.gradle.kts

repositories {
    maven { url = uri("https://jitpack.io") }
}

dependencies {
    // ...
    implementation(libs.usb.serial.for.android)
    // ...
}
```

### 2. Android 清单文件 (`app/src/main/AndroidManifest.xml`)

声明了 USB 主机功能，并设置了意图过滤器，以便在硬件插入时能识别设备。

```xml
<!-- app/src/main/AndroidManifest.xml -->
<manifest ...>

    <uses-feature android:name="android.hardware.usb.host" />

    <application ...>
        <activity
            android:name=".MainActivity"
            ...>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>
    </application>

</manifest>
```

### 3. USB 设备过滤器 (`app/src/main/res/xml/device_filter.xml`)

一个全新的文件，用于指定我们 App 关心的 USB 设备的厂商 ID 和产品 ID。

**注意：您必须将 `vendor-id` 和 `product-id` 替换为真实值！**

```xml
<!-- app/src/main/res/xml/device_filter.xml -->
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        IMPORTANT: Replace vendor-id and product-id with the actual values.
        For example, for a CH340 chip, vendor-id might be "6790" and product-id "29987".
    -->
    <usb-device vendor-id="1234" product-id="5678" />
</resources>
```

### 4. 硬件管理器 (`app/src/main/java/com/example/juicemachine/data/hardware/HardwareManager.kt`)

这是通信功能的核心，负责连接、断开和发送命令。

```kotlin
// Full content of HardwareManager.kt
package com.example.juicemachine.data.hardware

import android.app.PendingIntent
// ... other imports
import java.io.IOException

class HardwareManager private constructor(...) : SerialInputOutputManager.Listener {

    // ... (content as implemented previously) ...

    fun sendMakeJuiceCommand(ice: Int, juice: Int, water: Int) {
        // ...
        val command = byteArrayOf(
            0xFF.toByte(),              // Packet Header
            0x00.toByte(),              // User command
            ice.toByte(),               // Ice amount
            juice.toByte(),             // Juice amount
            water.toByte(),             // Water amount
            0x00.toByte(),              // Reserved byte
            0xFE.toByte()               // Packet Trailer
        )
        write(command)
    }

    fun sendCleanCommand() {
        val command = byteArrayOf(
            0xFF.toByte(),              // Packet Header
            0x01.toByte(),              // Admin command
            0x00.toByte(), 0x00.toByte(), 0x00.toByte(), 0x00.toByte(), // Data
            0xFE.toByte()               // Packet Trailer
        )
        write(command)
    }
    
    // ... (rest of the file) ...
}
```

### 5. ViewModel (`app/src/main/java/com/example/juicemachine/ui/viewmodel/DrinkMenuViewModel.kt`)

`ViewModel` 现在持有 `HardwareManager` 的引用，并调用其方法来发送指令。

```kotlin
// ... in DrinkMenuViewModel.kt

class DrinkMenuViewModel(
    private val recipeRepository: RecipeRepository,
    private val hardwareManager: HardwareManager
) : ViewModel() {

    // ...

    fun confirmCustomization(recipe: Recipe, cupSize: String) {
        // ... (logic to get config) ...

        // *** Send the actual command to hardware ***
        hardwareManager.sendMakeJuiceCommand(
            ice = config.ice,
            juice = config.juice,
            water = config.water
        )
        
        // ... (update UI state) ...
    }

    fun cleanMachine() {
        Log.d("DrinkMenuViewModel", "Clean machine command triggered.")
        hardwareManager.sendCleanCommand()
    }

    // ...
}

class DrinkMenuViewModelFactory(
    private val repository: RecipeRepository,
    private val hardwareManager: HardwareManager
) : ViewModelProvider.Factory {
    // ...
}
```

### 6. 主界面 (`app/src/main/java/com/example/juicemachine/MainActivity.kt`)

最后，我们将所有东西在 `MainActivity` 中组装起来。

```kotlin
// ... in MainActivity.kt
class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContent {
            JuiceMachineTheme {
                // Get application instance to access singletons
                val application = this.application as JuiceMachineApplication
                // Create ViewModel using the factory
                val viewModel: DrinkMenuViewModel = viewModel(
                    factory = DrinkMenuViewModelFactory(
                        application.repository,
                        application.hardwareManager
                    )
                )
                JuiceMachineApp(viewModel)
            }
        }
    }
}

// ...

@Composable
fun JuiceMachineApp(viewModel: DrinkMenuViewModel) {
    // ...
    when (uiState.currentScreen) {
        // ...
        is Screen.AdminMenu -> AdminScreen(
            uiState = uiState,
            onNavigateBack = { viewModel.navigateTo(Screen.DrinkMenu) },
            onAddClick = viewModel::onAddClick,
            onEditClick = viewModel::onEditClick,
            onDeleteClick = viewModel::deleteRecipe,
            onCleanClick = viewModel::cleanMachine // <-- Final connection
        )
        // ...
    }
    // ...
}
``` 