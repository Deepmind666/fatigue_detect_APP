# 修改记录

本文档记录了对智能茶饮机应用进行的重要修改和架构演进。

## 1. 核心架构：动态7字节指令协议

为了实现最大的灵活性，应用最终采用了一套动态生成7字节指令的硬件通信协议。此协议取代了所有旧的、基于固定ID的指令体系。

### 1.1. 指令结构

指令的每一位都有明确的含义，由App根据用户的选择和后台配方动态生成。

`[FF] [类型] [水量] [通道1果浆] [通道2果浆] [通道3果浆] [FE]`

- **Byte 0 (Header)**: `FF` - 固定起始位。
- **Byte 1 (类型)**: 定义指令的来源和类型。
  - `0x01`: 管理员指令（用于测试制作）。
  - `0x02`: 用户指令（正常冰）。
  - `0x03`: 用户指令（去冰）。
- **Byte 2 (水量)**: 水的重量（克），直接由配方决定。
- **Byte 3 (通道1果浆)**: 如果配方使用通道1，则此为果浆重量（克），否则为 `0x00`。
- **Byte 4 (通道2果浆)**: 如果配方使用通道2，则此为果浆重量（克），否则为 `0x00`。
- **Byte 5 (通道3果浆)**: 如果配方使用通道3，则此为果浆重量（克），否则为 `0x00`。
- **Byte 6 (Footer)**: `FE` - 固定结束位。

### 1.2. 实现细节

- **数据库**: `Recipe` 实体中包含一个 `juiceChannel` 字段 (1, 2, or 3)，用于指定该配方使用的果浆通道。
- **后台管理**: 管理员界面可以查看和修改每种配方的`名称`、`剩余重量`、`果汁通道`以及中杯/大杯的`配方`。
- **指令生成**: `HardwareManager` 中的 `makeJuice()` 方法负责根据传入的 `Recipe` 和 `CupConfig` 动态构建上述7字节指令。
- **旧指令**: 所有旧的、功能固定的管理员指令（如一键清洗、加水等）均已 **废弃**，因为它们与新协议不兼容。

## 2. 核心配方

系统后台仅保留3种核心饮料配方，分别对应3个物理果浆通道。

| 产品名称 | 果汁通道 |
| :--- | :--- |
| 茉莉雪芽 | 1 |
| 柳橙百香 | 2 |
| 满杯桑葚 | 3 |

## 3. 历史架构（已废弃）

### 3.1. 固定指令ID架构
这是最早的架构，通过为每个（饮料+冰量）组合分配一个固定的 `commandId` 来生成指令。此架构因缺乏灵活性和高度依赖数据库ID而已被淘汰。

### 3.2. 7字节固定指令
一个中间阶段的架构，虽然使用了7字节指令，但其内容是固定的，依赖硬件端预存配方。也已被动态协议取代。

---
**文档更新于最近的重构之后** 