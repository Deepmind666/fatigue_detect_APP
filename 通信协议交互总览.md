# 智能果汁机 - 通信协议交互总览 (v1.2)

本文档是Android应用与STM32下位机进行通信的**唯一最终标准**。它定义了所有交互场景下的指令与响应协议。

- **下行指令 (App -> STM32)**: App向STM32发送的控制命令，采用 **7字节** 结构。
- **上行响应 (STM32 -> App)**: STM32向App返回的状态/数据，采用 **5字节** 结构。

---

## 核心交互流程与协议总表

下表详述了所有可能发生的交互场景，以及对应的下行指令和上行响应。

| 交互场景 | 交互环节 | 协议方向 | 数据包 (Hex) | 详细说明 |
| :--- | :--- | :--- | :--- | :--- |
| **1. 制作饮品** | App 发起请求 | **下行指令** | `FF 01/02 ... FE` | **指令码 `0x01` / `0x02`**: 用于制作饮品。<br>**核心数据 (字节3-6)**: 包含水、汁1、汁2、汁3的克重。 |
| *(续)* | STM32 正常完成 | **上行响应** | `FD 01/02 AA 00 FC` | **回显 `0x01`/`0x02`**: 响应针对饮品制作指令。<br>**状态码 `0xAA`**: 任务成功。 |
| | | | | |
| **2. 异常处理** | STM32 制作暂停 | **上行响应** | `FD 01/02 AB 00 FC` | **回显 `0x01`/`0x02`**: 响应针对饮品制作指令。<br>**状态码 `0xAB`**: 任务因异常暂停，App应弹窗询问用户。 |
| *(续)* | 用户选择 **继续制作** | **下行指令** | `FF 07 00 00 00 00 FE` | **指令码 `0x07` (新)**: App指示STM32从暂停处继续任务。 |
| *(续)* | 用户选择 **重新制作** | **下行指令** | `FF 04 00 00 00 00 FE` | **指令码 `0x04` (复用)**: 发送急停指令，以终止并重置STM32当前任务，App同步重置UI，等待用户发起新的制作流程。 |
| | | | | |
| **3. 管理员操作** | **一键清洗** | **下行指令** | `FF 03 00 00 00 00 FE` | **指令码 `0x03`**: 启动所有泵清洗5秒。 |
| *(续)* | STM32 清洗完成 | **上行响应** | `FD 03 AA 00 FC` | **回显 `0x03`**: 响应针对清洗指令。<br>**状态码 `0xAA`**: 任务成功。 |
| | **紧急停止** | **下行指令** | `FF 04 00 00 00 00 FE` | **指令码 `0x04`**: 立即停止所有电机。 |
| *(续)* | STM32 已停止 | **上行响应** | `FD 04 AA 00 FC` | **回显 `0x04`**: 响应针对急停指令。<br>**状态码 `0xAA`**: 任务成功。 |
| | **去皮** | **下行指令** | `FF 05 00 00 00 00 FE` | **指令码 `0x05`**: 传感器去皮清零。 |
| *(续)* | STM32 去皮完成 | **上行响应** | `FD 05 AA 00 FC` | **回显 `0x05`**: 响应针对去皮指令。<br>**状态码 `0xAA`**: 任务成功。 |
| | **称重** | **下行指令** | `FF 06 00 00 00 00 FE` | **指令码 `0x06`**: 请求当前重量。 |
| *(续)* | STM32 返回重量 | **上行响应** | `FD 06 AA [ww] FC` | **回显 `0x06`**: 响应针对称重指令。<br>**状态码 `0xAA`**: 任务成功。<br>**数据 `[ww]`**: 1字节的重量值 (0-255g)。 |
| | | | | |
| **4. 通用响应状态**| 操作失败 | **上行响应** | `FD [cc] AC 00 FC` | **回显 `[cc]`**: 响应针对的指令码。<br>**状态码 `0xAC`**: STM32发生未知错误，任务失败。 |
| | 指令无效 | **上行响应** | `FD [cc] AD 00 FC` | **回显 `[cc]`**: 响应针对的指令码。<br>**状态码 `0xAD`**: STM32无法识别App发送的指令 `[cc]`。 |

---
## 附录：v1.0 固化指令详情 (16条，永不修改)

#### **A.1 用户指令 (制作饮品)**
*此处的指令码 `0x01` 和 `0x02` 并不直接区分饮品种类，而是作为通用制作指令，通过后续的4字节配方数据来定义具体内容。*

| 饮品名称 | 口味 | 核心数据 (字节 3-6) | 完整7字节指令 (Hex) |
| :--- | :--- | :--- | :--- |
| **茉莉雪芽** | 中杯-正常冰/去冰 | `69 AF 00 00` | `FF 01/02 69 AF 00 00 FE` |
| | 大杯-正常冰/去冰 | `87 F5 00 00` | `FF 01/02 87 F5 00 00 FE` |
| **柳橙百香** | 中杯-正常冰/去冰 | `B4 00 64 00` | `FF 01/02 B4 00 64 00 FE` |
| | 大杯-正常冰/去冰 | `D2 00 AA 00` | `FF 01/02 D2 00 AA 00 FE` |
| **满杯桑葚** | 中杯-正常冰/去冰 | `82 00 00 96` | `FF 01/02 82 00 00 96 FE` |
| | 大杯-正常冰/去冰 | `A0 00 00 DB` | `FF 01/02 A0 00 00 DB FE` |

#### **A.2 管理员指令**
| 功能 | 完整7字节指令 (Hex) |
| :--- | :--- |
| 一键清洗 | `FF 03 00 00 00 00 FE` |
| 紧急停止 | `FF 04 00 00 00 00 FE` |
| 去皮 | `FF 05 00 00 00 00 FE` |
| 称重 | `FF 06 00 00 00 00 FE` | 