## 目标
- 将图片与默认配方切换到A版（柳橙百香/茉莉雪芽/鸭屎香柠檬茶），同时保持当前稳定逻辑：广告页全屏拉伸、保存→发送闭环。
- 逐页验证：广告页/下单页/后台页/编辑页；保存后立即下单，串口帧按最新值。

## 具体改动
1) 资源与名称映射
- `AppNavigation.getBuiltInAds`：改为A版横图（`liu_cheng_bai_xiang_ad`、`mo_li_xue_ya_ad`、`ya_shi_xiang_ad`）。
- `DrinkMenuScreen/AdminScreen/EditRecipeScreen`：名称→资源名改为动态解析，优先A版名称；不存在时占位图。保持此前的动态解析模式，避免硬编码删除导致崩溃。

2) 默认配方（仅空库插入）
- `JuiceMachineApplication.ensureDefaultRecipesAsync`：默认三款改为A版，仍只在空库插入，不覆盖用户数据；速度兜底修正保留。

3) 保存与发送闭环保持
- 字段级更新（`RecipeDao.updateRecipeFields`）
- 保存后强校验再读库刷新（`DrinkMenuViewModel`）
- 下单前强读库最新+11字节新规发送（`HardwareManager`）
- 发送前核对日志（轻量、可开关）：打印将发送的水/果汁/速度/通道与订单id

4) 广告页
- 保持当前“单图全屏拉伸（FillBounds）”，禁用Glow/Stretch；启动清理Coil缓存逻辑保留。

## 验证步骤
- 图片：广告页三张A版横图全屏显示；下单/后台/编辑页显示A版竖图与名称。
- 保存：修改通道/水/果汁/速度→保存→列表即时更新；立即下单，串口帧为最新字段（日志核对）。
- 完成帧：订单状态更新为完成；无广告相关干扰。

## 风险控制
- 资源缺失：动态解析回退占位图并日志警告，不影响功能。
- 默认配方插入异常：捕获异常，不影响已有数据。
- 日志开关：可随时关闭核对日志，避免现场IO压力。

## 交付
- 完成代码切换与构建
- 执行现场验证脚本（保存→下单→广告显示）并输出确认日志
- 如有细节需微调（名称/文件名），快速按你现场反馈修正