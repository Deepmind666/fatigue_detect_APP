### 项目边界与当前结论

- **范围**: 仅检查与实现 Android 端串口通信与指令交互；硬件端（STM32）不在本方范围。
- **现状**:
  - 下行指令（App→设备）均已实现，三款饮品的中/大杯、正常/去冰会正确生成并发送7字节命令。
  - 上行指令（设备→App）已实现“固定识别一条异常帧”与两个文本回执：`FF 09 00 00 00 00 FE` → 弹窗；`TEST_OK`/`MAKE_COMPLETE` → Toast。
  - 已移除 UI 的“模拟杯子移动检测”按钮，现仅依赖串口收发驱动弹窗与操作。

### 串口通信参数

- 波特率: 9600
- 数据位: 8
- 校验位: 无（N）
- 停止位: 1

### 代码关键位置

- 串口与命令封装: `app/src/main/java/com/example/juicemachine/data/hardware/HardwareManager.kt`
- 业务调度与继续/重新制作: `app/src/main/java/com/example/juicemachine/ui/viewmodel/DrinkMenuViewModel.kt`
- 点单页与异常弹窗: `app/src/main/java/com/example/juicemachine/ui/DrinkMenuScreen.kt`
- 导航与管理员页: `app/src/main/java/com/example/juicemachine/ui/AppNavigation.kt`, `.../ui/AdminScreen.kt`

### 7字节下行指令（App → 设备）

| 功能 | 指令码 | 帧格式（Hex） |
| :-- | :--: | :-- |
| 制作饮品（正常冰） | 0x01 | `FF 01 [water] [ch1] [ch2] [ch3] FE` |
| 制作饮品（去冰） | 0x02 | `FF 02 [water] [ch1] [ch2] [ch3] FE` |
| 一键清洗 | 0x03 | `FF 03 00 00 00 00 FE` |
| 紧急停止 | 0x04 | `FF 04 00 00 00 00 FE` |
| 去皮 | 0x05 | `FF 05 00 00 00 00 FE` |
| 称重 | 0x06 | `FF 06 00 00 00 00 FE` |
| 继续制作 | 0x07 | `FF 07 00 00 00 00 FE` |
| 重新制作 | 0x08 | `FF 08 00 00 00 00 FE` |

- 映射规则（制作）：
  - 字节[2]=水克重（低8位），字节[3]/[4]/[5]在“所选果汁通道”填入果汁克重（低8位），其余通道填`00`。
  - 大杯加量：水+30，果汁+70；超出255仅发送低8位。

### 默认三款饮品（启动时若不存在则插入）

- 茉莉雪芽：水=105，汁=175，通道=1 → 果汁写入[3]
- 柳橙百香：水=180，汁=100，通道=2 → 果汁写入[4]
- 满杯桑葚：水=130，汁=150，通道=3 → 果汁写入[5]

示例（中/大杯 × 正常/去冰）：

- 茉莉雪芽（通道1）
  - 中杯 正常/去冰: `FF 01/02 69 AF 00 00 FE`
  - 大杯 正常/去冰: `FF 01/02 87 F5 00 00 FE`
- 柳橙百香（通道2）
  - 中杯 正常/去冰: `FF 01/02 B4 00 64 00 FE`
  - 大杯 正常/去冰: `FF 01/02 D2 00 AA 00 FE`
- 满杯桑葚（通道3）
  - 中杯 正常/去冰: `FF 01/02 82 00 00 96 FE`
  - 大杯 正常/去冰: `FF 01/02 A0 00 00 DC FE`

### 上行接收指令（设备 → App）

- 固定异常帧（触发弹窗）：`FF 09 00 00 00 00 FE`
- 文本回执（Toast）：包含`TEST_OK`、`MAKE_COMPLETE`
- 说明：未实现通用二进制帧解析，仅按需求固定识别 0x09 一条异常帧。

### 用户操作与数据流

- 点单页选择饮品 → 选择杯型/冰度 → 发送制作指令
- 设备在制作异常时发送固定异常帧 → 弹出“重量变化”对话框
  - 继续制作：发送 `FF 07 00 00 00 00 FE`
  - 重新制作：发送 `FF 08 00 00 00 00 FE`
- 管理员界面提供：清洗/停止/去皮/称重（发送 0x03~0x06）

### 已实施改动（UI）

- 删除 “模拟杯子移动检测” 按钮：
  - 文件：`app/src/main/java/com/example/juicemachine/ui/DrinkMenuScreen.kt`
  - 同步签名更新：`AppNavigation.kt`、预览函数

### 验证要点（现场/联调）

- 下发验证：操作后在 Logcat 过滤 `HardwareManager`，确认 `Sending command: ...` 与上表一致
- 接收验证：向 App 回送 `FF 09 00 00 00 00 FE` → 应弹出“重量变化”对话框
- 继续/重新制作：在弹窗点击相应按钮 → 观察发送 `FF 07 ...` 或 `FF 08 ...`

### 约束与注意事项

- 串口需先连接成功（`isConnected=true`），否则会提示“串口未连接”。
- `juiceChannel` 必须为 1/2/3；否则相应通道字节为 `00`。
- 克重使用低8位发送；建议控制“水≤225、果汁≤185”，以避免大杯加量后溢出。
- Toast 仅用于调试提示，不影响业务流程，可忽略其显示与否。

### 交付口径

- 本版本满足“固定识别一条异常帧 + 基础制作/管理员/继续/重新制作”的要求，不引入复杂协议解析与额外功能。

（记录人：Android 端）


